#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i, a;

    printf("Masukkan nilai: ");
    scanf("%d", &a);
    
    int b[a];
    for ( i = 0; i < a; i++)
    {

        if (b[1]%2!=0){
            b[a];
        }
        else if(b[1]%2==0){
            scanf ("%i", &b[i]);
        }
    }
    return 0;
}
